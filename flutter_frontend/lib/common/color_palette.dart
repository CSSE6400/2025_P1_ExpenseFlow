// Flutter imports
import 'package:flutter/material.dart';

class ColorPalette {
  // Primary Action Colors
  static const Color primaryAction = Color(0xFF0C2D62);      // Light mode
  static const Color primaryActionDark = Color(0xFF3B82F6);  // Dark mode

  // Secondary Action Colors
  static const Color secondaryAction = Color(0xFF0D9488);         // Light mode
  static const Color secondaryActionDark = Color(0xFF14B8A6);     // Dark mode

  // Accent Colors
  static const Color accent = Color(0xFF10B981);           // Light mode
  static const Color accentDark = Color(0xFF22C55E);       // Dark mode

  // Primary Text Colors
  static const Color primaryText = Color(0xFFF3F4F6);      // Light mode
  static const Color primaryTextDark = Color(0xFF0C2D62);  // Dark mode

  // Secondary Text Colors
  static const Color secondaryText = Color(0xFF6B7280);    // Light mode
  static const Color secondaryTextDark = Color(0xFF9CA3AF); // Dark mode

  // Background Colors
  static const Color background = Color(0xFFF3F4F6);        // Light mode
  static const Color backgroundDark = Color(0xFF0A0F1A);    // Dark mode

  // Error Color
  static const Color error = Color(0xFFEF4444);

  // Logo Color
  static const Color logoColor = Color(0xFF0C2D62);

  // Button Text Colors
  static const Color buttonText = Color(0xFFFFFFFF);       // Light mode
  static const Color buttonTextDark = Color(0xFF0C2D62);   // Dark mode
}